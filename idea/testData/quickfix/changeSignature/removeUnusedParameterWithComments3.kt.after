// "Remove parameter 'bar'" "true"
private fun foo(foo: String, baz: String = "") {}

private fun test() {
    foo(
        "foo", // foo comment
        "baz" // bar comment
        // baz comment
    )
}