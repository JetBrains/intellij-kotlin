// "Remove parameter 'foo'" "true"
private fun foo(bar: String, baz: String = "") {}

private fun test() {
    foo(
        "bar", // foo comment
        "baz" // bar comment
        // baz comment
    )
}